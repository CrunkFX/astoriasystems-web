---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className, hover = true } = Astro.props;
---

<div
  class:list={[
    'rounded-2xl glass p-6',
    hover && 'glass-hover',
    className,
  ]}
>
  <slot />
</div>
