---
import type { Locale } from '../../i18n/config';
import { routeMap } from '../../i18n/config';
import { t } from '../../i18n/utils';
import Container from '../ui/Container.astro';
import Section from '../ui/Section.astro';
import Button from '../ui/Button.astro';
import GlassCard from '../ui/GlassCard.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const routes = routeMap[locale];
---

<Section variant="alt" class="!py-24 lg:!py-32 overflow-hidden">
  <Container>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      <!-- Content side -->
      <div data-reveal="left">
        <span class="inline-block text-sm font-medium text-[var(--color-accent)] uppercase tracking-widest mb-4">
          {locale === 'de' ? 'Über uns' : 'About us'}
        </span>
        <h2 class="text-4xl md:text-5xl font-bold mb-6" data-text-reveal>{t(locale, 'about.title')}</h2>
        <p class="text-xl text-[var(--color-accent)] font-medium mb-6">{t(locale, 'about.subtitle')}</p>
        <p class="text-lg text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)] mb-4 leading-relaxed">
          {t(locale, 'about.description')}
        </p>
        <p class="text-lg text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)] mb-10 leading-relaxed">
          {t(locale, 'about.founded')}
        </p>
        <Button href={routes.about} variant="ghost" size="lg">
          {t(locale, 'about.cta')}
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
          </svg>
        </Button>
      </div>

      <!-- Image + stats side -->
      <div data-reveal="right">
        <!-- Workspace illustration -->
        <div class="mb-6" data-image-reveal>
          <div class="rounded-2xl overflow-hidden bg-gradient-to-br from-slate-50 via-slate-100/80 to-slate-50 dark:from-[#080c18] dark:via-[#0c1020] dark:to-[#080c18] flex items-center justify-center p-12" style="aspect-ratio: 4/3;">
            <svg viewBox="0 0 120 120" fill="none" class="w-full h-full max-w-[180px] max-h-[180px] text-[var(--color-accent-dark)] dark:text-[var(--color-accent)]">
              <polygon points="60,15 25,55 95,55" stroke="currentColor" stroke-width="1.5" fill="currentColor" fill-opacity="0.06" />
              <polygon points="40,55 60,30 80,55" stroke="currentColor" stroke-width="1" fill="currentColor" fill-opacity="0.04" />
              <line x1="60" y1="15" x2="30" y2="85" stroke="currentColor" stroke-width="0.75" opacity="0.2" />
              <line x1="60" y1="15" x2="90" y2="85" stroke="currentColor" stroke-width="0.75" opacity="0.2" />
              <line x1="25" y1="55" x2="95" y2="55" stroke="currentColor" stroke-width="0.75" opacity="0.2" />
              <circle cx="60" cy="15" r="3" fill="currentColor" opacity="0.6" />
              <circle cx="25" cy="55" r="2" fill="currentColor" opacity="0.4" />
              <circle cx="95" cy="55" r="2" fill="currentColor" opacity="0.4" />
              <circle cx="30" cy="85" r="2" fill="currentColor" opacity="0.3" />
              <circle cx="90" cy="85" r="2" fill="currentColor" opacity="0.3" />
              <circle cx="60" cy="95" r="1.5" fill="currentColor" opacity="0.25" />
              <line x1="30" y1="85" x2="90" y2="85" stroke="currentColor" stroke-width="0.75" opacity="0.15" />
              <line x1="30" y1="85" x2="60" y2="95" stroke="currentColor" stroke-width="0.5" opacity="0.1" />
              <line x1="90" y1="85" x2="60" y2="95" stroke="currentColor" stroke-width="0.5" opacity="0.1" />
            </svg>
          </div>
        </div>

        <!-- Stats grid -->
        <div class="grid grid-cols-2 gap-4">
          <GlassCard class="text-center py-5">
            <div class="text-3xl font-bold gradient-text mb-1">2025</div>
            <div class="text-sm text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)]">{locale === 'de' ? 'Gegründet' : 'Founded'}</div>
          </GlassCard>
          <GlassCard class="text-center py-5">
            <div class="text-3xl font-bold gradient-text mb-1">360°</div>
            <div class="text-sm text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)]">{locale === 'de' ? 'Ganzheitlich' : 'Holistic'}</div>
          </GlassCard>
          <GlassCard class="text-center py-5">
            <div class="text-3xl font-bold gradient-text mb-1">DSGVO</div>
            <div class="text-sm text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)]">{locale === 'de' ? 'Konform' : 'Compliant'}</div>
          </GlassCard>
          <GlassCard class="text-center py-5">
            <div class="text-3xl font-bold gradient-text mb-1">EU</div>
            <div class="text-sm text-[var(--color-text-secondary)] dark:text-[var(--color-dark-text-secondary)]">{locale === 'de' ? 'Hosting' : 'Hosted'}</div>
          </GlassCard>
        </div>
      </div>
    </div>
  </Container>
</Section>
